# R01 AI Review

## Scope
- 사진 5×5 좌표 저장
- 작성·편집 드래그 재배치
- 상세 상하좌우 탐색

## Findings
- 사진은 최대 20장이고 25개 슬롯에 빈 칸을 포함해 배치할 수 있다.
- 포인터 드래그 경로의 슬롯들이 순서대로 비켜나며 FLIP 애니메이션으로 이동한다.
- 현재 좌표에서 정확히 한 칸 인접한 사진 방향에만 화살표가 표시된다.
- 기존 게시글 좌표가 원격 migration으로 backfill됐다.
- 서버 초기 HTML에 저장 좌표가 포함돼 새로고침 기본 배치 노출이 없다.

## Verification
- Supabase migration `20260730160000` remote 적용
- 기존 `photo_posts.image_layout` backfill 조회
- `npx tsc --noEmit`
- `npm run lint`
- `npm run build`
- `/gallery` server HTML `PhotoPosts`, `ImageLayout`, 저장 좌표 확인

## Decision
- Approved.
